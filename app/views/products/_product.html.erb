<article>
  <%= link_to product_path(product), class: "product-name" do %>
    <%= image_tag product.image.thumb.url , alt: product.name %>
    <h1>
      <span><%= product.name %></span>
      <span><%= humanized_money_with_symbol product.price %></span>
    </h1>
  <% end%>
  
  <div>
  <% unless product.quantity > 0 %>
  <div class='soldout'>Sold out</div>
  <%= button_to add_item_cart_path(product_id: product.id), class: 'btn', disabled: true ,
    method: :post do %>
      <%= fa_icon "shopping-cart", text: 'Add' %> 
    <% end %>
  <% else %>
    <%= button_to add_item_cart_path(product_id: product.id), class: 'btn',
    method: :post do %>
      <%= fa_icon "shopping-cart", text: 'Add' %> 
    <% end %>
  <% end %>
  </div>
</article>
